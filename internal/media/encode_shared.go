package media

// PreferredImageExtension returns the file extension used for encoded media assets.
func PreferredImageExtension() string {
	return preferredImageExtension
}

// PreferredImageMIME returns the MIME type associated with encoded media assets.
func PreferredImageMIME() string {
	return preferredImageMime
}
